# Since .env is gitignored, you can use .env-example to build a new `.env` 
# file when you clone the repo.
# Keep this file up-to-date when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to
# have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env`
# and populate it with your secrets.

# When adding additional env variables, the schema in /env/schema.mjs
# should be updated accordingly

# Prisma
#   * Prisma can run this off a sqlite db for local development
#DATABASE_URL=file:./db.sqlite

# Developer server
DATABASE_URL=<postgresql:// url>

# A given PR, for example
# DATABASE_URL=<postgresql:// url>

# Production (playing with fire)
# DATABASE_URL=<postgresql:// url>

# Next Auth
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# GitHub OAuth
GITHUB_ID=
GITHUB_SECRET=

AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
# 👇 looks like a https url with your username in it
AUTH0_ISSUER=

